import { Typography } from "@mui/material";
import BlockChain from "../components/BlockChain";


export default function Basic(props) {
    return (
        <div>
            <Typography variant="h2">Basic Blockchain</Typography>
            <BlockChain />
        </div>
    )
}